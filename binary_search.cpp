#include <stdio.h>
int binarysearch(int arr[],int n,int K){
    int low = 0,high = n,mid=(low+high)/2;
    while (low<high){
    {
        if(mid==40)
            return mid;
    }
    
}

int main( ){
    int arr [ ] = { 10,20,30,40,50,60,70,80,100,200,300,400,500 },k=50 ;
    int size = sizeof ( arr ) / sizeof ( int ) ;
    printf("Present at index :  %d\n",binarysearch ( arr, size,k ) ;
    return 0;
}